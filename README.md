## วิธีติดตั้ง (Installation)
1. ติดตั้ง Library ที่จำเป็น:
   pip install -r requirements.txt
## วิธีใช้งาน (Usage)
1. รันคำสั่ง:
   streamlit run app.py
   ## 📝 คำอธิบายการทำงานของโค้ด (Code Explanation)

โปรเจกต์นี้พัฒนาด้วยภาษา Python โดยใช้เฟรมเวิร์ก Streamlit ซึ่งมีหลักการทำงานแบ่งเป็น 4 ส่วนหลักดังนี้:

### 1. การจัดการข้อมูล (Data Handling)
* **Pandas DataFrame:** ใช้สำหรับสร้างและจัดการชุดข้อมูลจำลอง (Mock Data) ประกอบด้วยหมวดหมู่สินค้า (`Category`), ยอดขาย (`Sales`), กำไร (`Profit`) และเดือนที่ทำรายการ (`Month`)
* **Data Filtering:** ใช้คำสั่ง `df[df['Category'].isin(selected_cat)]` เพื่อกรองข้อมูลจาก DataFrame หลัก ให้เหลือเฉพาะหมวดหมู่ที่ผู้ใช้เลือกจาก Sidebar

### 2. ส่วนควบคุม (Interactive Sidebar)
* **Multiselect Widget:** สร้างตัวเลือกแบบรายการ (Drop-down) ให้ผู้ใช้สามารถเลือกดูข้อมูลเฉพาะหมวดหมู่ที่สนใจได้แบบ Real-time
* **State Management:** เมื่อผู้ใช้เปลี่ยนตัวเลือก Streamlit จะทำการรันโค้ดใหม่ทั้งหมด (Rerender) ทำให้ข้อมูลในกราฟอัปเดตตามทันที

### 3. การแสดงผลข้อมูล (Data Visualization)
* ใช้ไลบรารี **Plotly Express** ในการสร้างกราฟ 3 รูปแบบ:
    * **Bar Chart:** แสดงยอดขายเปรียบเทียบระหว่างหมวดหมู่สินค้า แยกสีตามเดือน
    * **Pie Chart:** แสดงสัดส่วนกำไร (Profit Distribution) ของแต่ละหมวดหมู่
    * **Line Chart:** แสดงแนวโน้ม (Trend) ของยอดขายในแต่ละเดือน
* **Layout Management:** ใช้ `st.columns(2)` เพื่อจัดวางกราฟให้ขนานกันในแนวนอน ช่วยให้แดชบอร์ดดูสะอาดตาและใช้งานง่าย

### 4. ส่วนเสริมและฟังก์ชันอื่น ๆ
* **Metric Cards:** แสดงตัวเลขสรุปยอดขายและกำไรรวมทั้งหมด (Total) เพื่อให้เห็นภาพรวมได้รวดเร็ว
* **Export Function:** ผู้ใช้สามารถดาวน์โหลดข้อมูลที่กรองแล้วออกมาเป็นไฟล์ `.csv` ผ่านปุ่ม Download ได้